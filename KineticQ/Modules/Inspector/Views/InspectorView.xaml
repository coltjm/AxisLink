<UserControl x:Class="KineticQ.Modules.Inspector.Views.InspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:KineticQ.Modules.Machines.ViewModels"
             xmlns:editors="clr-namespace:KineticQ.Modules.Inspector.Editors">

    <Grid>

        <TextBlock Text="INSPECTOR"  Margin="10"/>

        <ContentControl Grid.Row="1" Content="{Binding SelectedContext}" Margin="10,40,10,10">
            <ContentControl.Resources>

                <DataTemplate DataType="{x:Type vm:MachineViewModel}">
                    <editors:MachineEditorView />
                </DataTemplate>

                <DataTemplate DataType="{x:Type cvm:CueViewModel}" 
                              xmlns:cvm="clr-namespace:KineticQ.Modules.Cueing.ViewModels"
                              xmlns:ceditors="clr-namespace:KineticQ.Modules.Cueing.Views">
                    <editors:CueEditorView />
                    </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <TextBlock Grid.Row="1" Text="No Selection" 
                   Visibility="{Binding SelectedContext, Converter={StaticResource NullToVisConverter}}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
    </Grid>
</UserControl>