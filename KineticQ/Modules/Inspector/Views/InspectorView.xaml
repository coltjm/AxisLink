<UserControl x:Class="KinetiCUE.Modules.Inspector.Views.InspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:KinetiCUE.Modules.Machines.ViewModels"
             xmlns:viewModels="clr-namespace:KinetiCUE.Modules.Machines.ViewModels"
             xmlns:editors="clr-namespace:KinetiCUE.Modules.Inspector.Editors"
             xmlns:cvm1="clr-namespace:KinetiCUE.Modules.Cueing.ViewModels">

    <Grid>

        <TextBlock Text="INSPECTOR"  Margin="10"/>

        <ContentControl Grid.Row="1" Content="{Binding SelectedContext}" Margin="10,40,10,10">
            <ContentControl.Resources>

                <DataTemplate DataType="{x:Type viewModels:MachineViewModel}">
                    <editors:MachineEditorView />
                </DataTemplate>

                <DataTemplate DataType="{x:Type cvm1:CueViewModel}" 
                              xmlns:cvm="clr-namespace:KinetiCUE.Modules.Cueing.ViewModels">
                    <editors:CueEditorView />
                    </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>

        <TextBlock Grid.Row="1" Text="No Selection" 
                   Visibility="{Binding SelectedContext, Converter={StaticResource NullToVisConverter}}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextMuted}"/>
    </Grid>
</UserControl>