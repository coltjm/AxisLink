<UserControl x:Class="KinetiCUE.Modules.Cueing.Views.CueListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:KinetiCUE.ViewModels">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Cues}"
                  SelectedItem="{Binding SelectedCue}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="Label" Binding="{Binding Label}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Trigger" Binding="{Binding Trigger}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Wait" Binding="{Binding TriggerDelay, StringFormat=N2}" Width="60" IsReadOnly="True"/>
            </DataGrid.Columns>

            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Background="#101010" Padding="20,5,5,10" BorderBrush="#333" BorderThickness="0,1,0,0">
                        <StackPanel>
                            <TextBlock Text="MOTOR INSTRUCTIONS" 
                                       FontSize="9" 
                                       Foreground="{StaticResource TextMuted}" 
                                       Margin="0,0,0,4"
                                       FontWeight="Bold"/>

                            <ItemsControl ItemsSource="{Binding Instructions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="150"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="100"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                                <TextBlock Text="↳ " Foreground="{StaticResource BrandPurple}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding MachineName}" Foreground="#CCC"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="1" Text="{Binding TargetPosition, StringFormat='To: {0:N2}'}" Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Duration, StringFormat='Time: {0:N1}s'}" Foreground="{StaticResource TextMuted}"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="No motors assigned." 
                                       FontSize="10" 
                                       Foreground="#444" 
                                       FontStyle="Italic"
                                       Visibility="{Binding Instructions.Count, Converter={StaticResource ZeroToVisConverter}}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>

        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <Button Content="Add Cue" Style="{StaticResource FlatButton}" Command="{Binding AddCueCommand}" Margin="0,0,10,0"/>
            <Button Content="Delete" Style="{StaticResource FlatButton}" Command="{Binding DeleteCueCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>